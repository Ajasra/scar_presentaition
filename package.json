{
  "name": "scar-presentation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run update-data",
    "build": "vite build",
    "preview": "vite preview --host",
    "update-data": "node scripts/build-concepts-db.js"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
