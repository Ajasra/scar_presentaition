{
  "name": "scar-presentation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8745",
    "prebuild": "npm run update-data",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8745 --strictPort",
    "start": "vite preview --host 0.0.0.0 --port 8745 --strictPort",
    "update-data": "node scripts/build-concepts-db.js"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
